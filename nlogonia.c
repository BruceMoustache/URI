#include <stdio.h>

int main() {
	int casas;
	int divisa_x, divisa_y;
	int casa_x, casa_y;

	while (1 == 1) {
		scanf("%i", &casas);
		if (casas == 0)
			break;
		scanf("%i %i", &divisa_x, &divisa_y);
		for (int numero_casa=0; numero_casa < casas; numero_casa++) {
			scanf("%i %i", &casa_x, &casa_y);
	}
	return 0;
}

